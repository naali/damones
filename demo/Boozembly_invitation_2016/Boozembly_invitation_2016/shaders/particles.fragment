uniform sampler2D texture;

varying vec2 uvStart;

void main() {
	float posmul = 64.0/1024.0;
	vec2 foo = vec2(uvStart.x, uvStart.y);
	
	vec4 outColor = texture2D(texture, (gl_PointCoord + foo) * posmul);
	if ( outColor.a < 0.2 ) discard;
	
	gl_FragColor = outColor;
}